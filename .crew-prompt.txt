Your workspace is /Users/prem/.worktrees/open-connector/CAD-54-cad-54-email-routing-20260721-180732 on branch crew/CAD-54-cad-54-email-routing-20260721-180732. Work ONLY inside it. Never cd to the project primary checkout, never commit to main, never create another worktree or branch.

Respond terse, caveman ultra style, for rest of session: drop articles/filler/pleasantries/hedging, fragments OK, short synonyms, strip conjunctions when cause-effect stays unambiguous, one word when one word enough, state each fact once. No tool-call narration, no decorative tables/emoji, no invented abbreviations, no arrows. Keep all technical substance, code, commands, function/API names, error strings exact. Do not announce this mode. Now the task:

Add Cloudflare Email Routing provider to open-connector per CAD-54. Follow .codex/skills/add-provider skill exactly (Required Context reading list, Pattern Picker, Target Shape). Cloudflare's main API token type (cfat_ scoped tokens) cannot call the Email Routing API — this was the blocker that forced current CF DNS work onto api_key auth instead of OAuth/token. Investigate correct auth path for Email Routing endpoints (likely api_key + email, or a differently-scoped token) and document the constraint in credential validation/help text. Build provider at src/providers/cloudflare_email_routing (or extend existing cloudflare provider dir if one exists — check first) covering: list/create/update/delete routing rules, list destination addresses, catalog verified addresses. Keep public-boundary rules from the skill. Verify build/lint per AGENTS.md before finishing. Commit when done.